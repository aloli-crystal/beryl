= Hébergeurs de serveurs dédiés (bare metal) avec API
:toc: left
:toclevels: 3
:sectnums:
:icons: font
:source-highlighter: rouge
:experimental:

[.lead]
Liste des hébergeurs proposant à la location des serveurs *physiques complets* (bare metal / dédiés), et non des machines virtuelles, avec une *API publique de gestion*.

IMPORTANT: Les prix et caractéristiques sont susceptibles d'évoluer. Référez-vous toujours au site officiel de chaque prestataire pour les informations à jour. Les informations ci-dessous sont valables en avril 2026.

== Critères retenus

* Location de serveurs *bare metal* (matériel physique entièrement dédié au locataire, sans hyperviseur imposé).
* *API publique documentée* permettant au minimum le provisioning, le reboot, la réinstallation OS, la gestion réseau.
* Sont exclus les VPS, instances virtualisées et offres uniquement via ticket/courriel.

== Hébergeurs français

[cols="2,1,2,3,2,3", options="header"]
|===
| Hébergeur | Pays | Siège | API | Datacenters | Particularités

| *OVHcloud*
| 🇫🇷 France
| Roubaix
| REST (API v1/v2), CLI, Terraform provider officiel
| 13 régions, 46 DC dans le monde
| Leader européen. Gamme très large (Rise, Advance, Scale, High Grade, HGR-AI). Offre Bare Metal Pod qualifiée SecNumCloud administrable via API OpenStack.

| *Scaleway* (ex-Online.net / Dedibox)
| 🇫🇷 France
| Paris
| REST, CLI, Terraform, SDK Go/Python/JS
| France (Paris), Pays-Bas (Amsterdam)
| Trois gammes : *Dedibox* (dédiés classiques), *Elastic Metal* (bare metal à la demande, facturation horaire, provisioning en minutes), *Apple Silicon* (Mac mini M1/M2/M4).

| *Ikoula* (groupe Sewan)
| 🇫🇷 France
| Reims / Eppes
| API REST (extranet), plus limitée que les leaders
| France, Allemagne, Pays-Bas
| Acteur historique (1998). Livraison en 1h, gammes Power, GPU, Proxmox, Synology. API moins riche que OVH/Scaleway.

| *Outscale* (Dassault Systèmes)
| 🇫🇷 France
| Saint-Cloud
| API compatible AWS EC2 + OpenStack
| France, USA, Chine, Inde
| Qualifié SecNumCloud. Offre orientée cloud souverain, bare metal disponible via « Dedicated Cloud ».

| *Phoenix* / *PulseHeberg* / *Hopus* (petits acteurs)
| 🇫🇷 France
| France
| API variable selon le prestataire
| France
| À étudier au cas par cas, offres nichées (gaming, etc.). Vérifiez toujours la présence effective d'une API.
|===

== Hébergeurs européens (hors France)

[cols="2,1,2,3,2,3", options="header"]
|===
| Hébergeur | Pays | Siège | API | Datacenters | Particularités

| *Hetzner*
| 🇩🇪 Allemagne
| Gunzenhausen
| Deux API distinctes : *Robot Webservice* (dédiés traditionnels) et *Cloud API* (Cloud). Terraform provider, SDK Python/Ruby/Go communautaires.
| Allemagne, Finlande, USA, Singapour
| Excellent rapport qualité/prix. Datacenters alimentés à 100 % en énergie renouvelable. *Server Auction* pour serveurs d'occasion à prix cassés.

| *IONOS*
| 🇩🇪 Allemagne
| Montabaur
| REST Cloud API
| Europe, USA
| Offres dédiées entreprise, tarifs transparents, trafic illimité 1 Gbit/s.

| *Leaseweb*
| 🇳🇱 Pays-Bas
| Amsterdam
| REST API, Terraform provider
| Europe, USA, Asie, Australie
| Réseau Tier-1 de haut niveau, IPMI/iDRAC/iLO inclus par défaut, gammes managées ou non.

| *Worldstream*
| 🇳🇱 Pays-Bas
| Naaldwijk
| API client (plus limitée)
| Pays-Bas, USA
| Réseau performant, options unmetered, bon rapport qualité/prix.

| *Cherry Servers*
| 🇱🇹 Lituanie
| Vilnius
| REST API complète, Terraform, Pulumi, CLI
| Lituanie, Pays-Bas, USA
| Provisioning API-first, facturation horaire/mensuelle, GPU disponibles. Alternative sérieuse à Equinix Metal.

| *Clouvider*
| 🇬🇧 Royaume-Uni
| Londres
| REST API
| UK, Europe, USA, Asie
| Réseau mondial de qualité, présent sur les IXP majeurs.

| *Snel.com*
| 🇳🇱 Pays-Bas
| Rotterdam
| API client
| Pays-Bas, USA
| Livraison rapide, tarifs abordables.
|===

== Hébergeurs mondiaux / internationaux

[cols="2,1,2,3,2,3", options="header"]
|===
| Hébergeur | Pays | Siège | API | Datacenters | Particularités

| *Latitude.sh* (ex-Maxihost)
| 🇧🇷 Brésil / 🇺🇸 USA
| São Paulo / USA
| REST API mature, Terraform provider, CLI
| Brésil, USA, Europe, Asie
| Provisioning en quelques secondes, très orienté développeurs. Se positionne comme le remplaçant naturel d'Equinix Metal.

| *Vultr*
| 🇺🇸 USA
| West Palm Beach, FL
| REST API v2 complète, SDK Python/Go/JS
| 30+ régions mondiales
| Bare metal à côté des VPS, facturation horaire, provisioning rapide.

| *PhoenixNAP* (Bare Metal Cloud)
| 🇺🇸 USA
| Phoenix, AZ
| REST API, Terraform, Ansible, Pulumi
| USA, Europe, Asie, Australie
| *API-first*, facturation horaire véritable, très utilisé en CI/CD et IaC.

| *Servers.com*
| 🇺🇸 USA
| USA
| REST API, Terraform
| USA, Europe, Asie
| Réseau privé entre serveurs, bonnes options GPU.

| *Hivelocity*
| 🇺🇸 USA
| Tampa, FL
| REST API, Terraform
| USA, Europe, Asie
| Nombreuses localisations, provisioning rapide.

| *Liquid Web*
| 🇺🇸 USA
| Lansing, MI
| REST API (Storm API)
| USA, Europe
| Gamme managée de référence, support 24/7 inclus.

| *InterServer*
| 🇺🇸 USA
| Secaucus, NJ
| API client
| USA
| Tarifs agressifs, accès root complet.

| *GTHost*
| 🇨🇦 Canada
| Canada
| API disponible
| Multiples villes
| Serveurs dédiés instantanés, facturation horaire ou mensuelle.

| *OneProvider*
| 🇫🇷 France (international, revendeur)
| Paris
| API
| ~150 villes via partenaires
| Agrégateur donnant accès à des dédiés dans de nombreuses villes peu couvertes ailleurs.

| *AWS* (EC2 bare metal : `*.metal` instances)
| 🇺🇸 USA
| Seattle, WA
| API AWS complète, SDK tous langages
| Mondial
| Instances `i3.metal`, `m5.metal`, etc. — véritables serveurs physiques, mais facturés comme des instances EC2.

| *Google Cloud* (Bare Metal Solution)
| 🇺🇸 USA
| Mountain View, CA
| API GCP
| Amérique du Nord, Europe, Asie
| Orienté charges SAP HANA, bases de données critiques.

| *IBM Cloud Bare Metal*
| 🇺🇸 USA
| Armonk, NY
| REST API, Terraform
| Mondial
| Historiquement SoftLayer, très personnalisable, orienté entreprise.

| *Oracle Cloud* (OCI Bare Metal)
| 🇺🇸 USA
| Austin, TX
| REST API OCI, Terraform
| Mondial
| Tarifs agressifs, mais bare metal exclu du Free Tier.

| *Azure Bare Metal*
| 🇺🇸 USA
| Redmond, WA
| API Azure, ARM templates, Terraform
| Mondial
| Intégration native avec l'écosystème Azure, orienté hybride.
|===

== Cas particuliers et points d'attention

=== Equinix Metal : fin de service

[WARNING]
====
*Equinix Metal* était historiquement la référence du bare metal API-first (plateforme Packet rachetée par Equinix). Equinix a annoncé la *fin commerciale* du service : plus de nouvelles ventes, et *sunset complet au 30 juin 2026*. Ne pas démarrer de nouveau projet sur cette plateforme.

Les alternatives les plus souvent citées pour une migration sont *Latitude.sh*, *Cherry Servers*, *PhoenixNAP*, *Vultr Bare Metal* et *Hetzner*.
====

=== Frontière bare metal / cloud

La distinction est parfois floue :

* *Bare metal classique* (OVH Rise, Hetzner dédiés, Dedibox) : livraison en minutes à heures, facturation mensuelle, très compétitif en prix.
* *Bare metal cloud* (Scaleway Elastic Metal, PhoenixNAP BMC, Latitude.sh, AWS `*.metal`, Vultr Bare Metal) : provisioning API en secondes/minutes, facturation horaire, intégration cloud-native.

Les deux modèles sont du bare metal au sens strict — un seul locataire sur le matériel physique — mais le second est plus agile et mieux adapté aux workflows IaC modernes.

== Synthèse : quel hébergeur pour quel usage ?

[cols="3,5", options="header"]
|===
| Besoin principal | Recommandation

| Souveraineté française, large catalogue
| OVHcloud, Scaleway

| SecNumCloud (données sensibles, OIV/OSE)
| OVHcloud Bare Metal Pod, Outscale

| Rapport prix/performance imbattable en Europe
| Hetzner (surtout Server Auction)

| API-first, Terraform, facturation horaire
| PhoenixNAP, Latitude.sh, Cherry Servers, Scaleway Elastic Metal

| Écosystème cloud intégré mondial
| AWS `*.metal`, Azure, GCP, OCI, IBM

| Alternative directe à Equinix Metal
| Latitude.sh, Cherry Servers, PhoenixNAP

| Proximité géographique (France, faible latence)
| OVHcloud, Scaleway, Ikoula
|===

== Compatibilité beryl

beryl provisionne un FreeBSD bare metal en orchestrant trois opérations
côté API hébergeur :

. Bascule du serveur en *rescue Linux* (Debian/Ubuntu, peu importe).
. *Reboot vers le disque* après l'installation FreeBSD (sinon le serveur
  reboot en rescue).
. *Gestion des clés SSH* (idéalement injection dynamique au rescue ;
  acceptable si figées à l'install initial avec la possibilité de relancer
  un install via API).

Trois providers sont câblés et testés terrain (24-25 avril 2026) :

[cols="2,1,3,3", options="header"]
|===
| Hébergeur | Statut | Forces | Limitations beryl

| *OVHcloud*
| ✅ Validé
| API rescue + boot-disk + SSH keys dynamiques. Tâches asynchrones pollables. UX la plus naturelle des trois.
| `service_name` textuel à mémoriser ; rescue task parfois lent (5-10 min).

| *Scaleway Elastic Metal*
| ✅ Validé (avec réserves)
| API REST très complète, reverse DNS IPv4/IPv6 par API, install par UUID.
| User rescue = `rescue` (pas root) → besoin d'un promote. Clés figées à `install.ssh_key_ids` initial → `beryl scaleway-reinstall` nécessaire si la clé n'a pas été cochée à la création. `boot_type` API à passer en `Normal` au reboot bare-metal sinon le serveur revient en rescue.

| *Dedibox / Online.net*
| ✅ Validé
| API simple Bearer, IAM keys auto-injectées en rescue.
| Reverse DNS NON exposé par API (panel uniquement). User rescue = `sd-<id>` → besoin d'un promote sudo. `prepare_rescue` + `reboot` sont deux étapes distinctes (helpers shard).
|===

=== Hébergeurs à investiguer pour la prochaine itération beryl

Listés par compatibilité présumée (API-first → vers panel-only), avec
ticket d'entrée :

[cols="2,1,2,4", options="header"]
|===
| Hébergeur | API-first | Ticket | Notes pour intégration beryl

| *PhoenixNAP* (Bare Metal Cloud)
| ⭐⭐⭐
| Horaire
| Cible naturelle pour beryl : API-first, Terraform/Ansible/Pulumi déjà câblés, facturation horaire (test peu coûteux). Alternative explicite à Equinix Metal qui ferme. À chiffrer côté Aloli.

| *Cherry Servers*
| ⭐⭐⭐
| Horaire/mensuel
| REST API + Terraform/Pulumi/CLI. Lituanie, donc UE. API jeune mais documentation propre.

| *Latitude.sh* (ex-Maxihost)
| ⭐⭐⭐
| Mensuel
| Provisioning « en quelques secondes ». BR/USA, datacenters Europe disponibles.

| *Hetzner Robot* (dédiés classiques)
| NOGO Aloli
| Mensuel + setup
| Robot Webservice = REST/JSON (la rumeur XML-RPC est fausse, vérifié 25 avril 2026 ; le shard `api-hetzner` parle JSON proprement). API utilisable, mais *Hetzner exige une copie de pièce d'identité à l'inscription* (vérifié 26 avril 2026) — politique disproportionnée pour de la location bare-metal, refusée par Aloli. Le shard `api-hetzner` reste publié pour la communauté Crystal qui voudrait l'utiliser.

| *Vultr Bare Metal*
| ⭐⭐
| Horaire
| API REST v2 mature, SDK officiels. Mais offre orientée `*.metal` cloud-style — à vérifier si rescue par API.

| *Outscale*
| ⭐
| Mensuel ou réservation
| Compatible AWS EC2 (FCU) + OpenStack ; le « Dedicated Cloud » est un single-tenant cloud, pas du vrai baremetal au sens des trois ci-dessus. Qualifié SecNumCloud, intéressant pour des clients OIV/OSE mais hors flow beryl actuel.

| *Ikoula*
| ⭐
| Mensuel
| API extranet « plus limitée que les leaders ». Demander la liste exhaustive des endpoints `wsds/` (rescue, boot-disk, ssh-keys) avant intégration. Acteur historique français, intéressant pour la souveraineté.

| *PulseHeberg* / *Hopus*
| ?
| Mensuel
| Petits acteurs français, offres nichées. À évaluer manuellement (présence d'API publique, niveau d'automatisation).

| *AWS `*.metal`* / *Azure* / *GCP* / *OCI*
| ⭐⭐⭐
| Horaire (cher)
| API très complète mais coût d'un baremetal cloud-public significatif (souvent plusieurs €/heure). Hors cible Aloli (privilégier la souveraineté FR/EU et le rapport coût-perf).
|===

=== Recommandation d'ordre d'intégration

. *PhoenixNAP* d'abord : facturation horaire = test peu coûteux,
  API-first donc le moins de friction côté code beryl, et la fenêtre
  Equinix Metal qui ferme nous donne une visibilité naturelle.
. *Cherry Servers* ensuite : Europe (Lituanie), API moderne, similaire à
  PhoenixNAP en termes d'effort d'intégration.
. *Hetzner Robot* : *écarté pour Aloli* — Hetzner réclame une
  copie de pièce d'identité à l'inscription (vérifié 26 avril 2026),
  contrainte refusée par Aloli pour de la location bare-metal. Le
  shard `api-hetzner` (REST/JSON, MIT) reste disponible pour
  la communauté Crystal qui voudrait l'utiliser. Toujours pertinent
  comme cible technique pour qui n'a pas ce souci.
. *Latitude.sh* / *Vultr Bare Metal* : nice-to-have, marché plus
  international.
. *Outscale* / *Ikoula* / *PulseHeberg* / *Hopus* : selon la demande
  client / la souveraineté requise. Pas prioritaire.

== Sources et vérification

Pour vérifier la présence effective d'une API chez un prestataire, cherchez sur son site :

* Une section `/developers`, `/api`, `/docs/api`.
* Un provider Terraform officiel sur le registry HashiCorp.
* Un SDK officiel sur GitHub (organisation du prestataire).

[TIP]
====
Pour votre usage avec Crystal, la plupart de ces API étant des REST JSON standard, vous pouvez les consommer directement avec la stdlib `HTTP::Client` + `JSON.parse`, ou via des shards génériques. À ma connaissance, aucun SDK Crystal officiel n'est fourni par ces prestataires — il faut soit consommer l'API en direct, soit wrapper un SDK existant.
====
