= Changelog
:toc: left
:toclevels: 2

Toutes les modifications notables de ce projet sont documentées dans ce fichier.

Le format s'inspire de https://keepachangelog.com/fr/1.1.0/[Keep a Changelog]
et ce projet suit https://semver.org/lang/fr/[Semantic Versioning].

== [Non publié]

=== Post-install sans chroot et conf FreeBSD par YAML (21 avril 2026)

==== Ajouté

* ADR-013 — *post-install sans chroot.* Le chroot `bsdinstall` déclenche
  un `SIGSYS` Capsicum sur `pkg install` (vérif de signature tuée par le
  sandbox). `beryl` ne l'utilise plus : l'installerconfig est devenu un
  préambule `ZFSBOOT_*` + `DISTRIBUTIONS=` seul, tout le post-install
  (users via `pw -R /mnt`, packages via `pkg -r /mnt`, rc.conf,
  timezone, sudoers) est piloté par mfsBSD hors chroot en ciblant
  explicitement `/mnt/`. Validé bout-en-bout sur `loulou.aloli.net`.
* `Beryl::Bootstrap::UserSpec` : record décrivant un user à créer
  (name, primary_group, secondary_groups, shell, ssh_keys) avec
  `validate!` + `to_tsv` pour le driver shell.
* `Beryl::Bootstrap::QemuInRescue.new` accepte désormais
  `disks : Array(String)`, `raid`, `users`, `packages`, `sudoers`
  (multi-disque et RAID ZFS stripe/mirror/raidz{,2,3}).
* Template `rescue-run-vm.sh` : driver shell complet qui fait tourner
  QEMU via `systemd-run --unit=qemu-vm` (survit à la déconnexion ssh),
  pilote mfsBSD en SSH, fait le post-install, puis `poweroff`.
* `beryl boot-hd <host>` : bascule un hôte OVH en boot disque via
  l'API (inverse de `rescue`). Utilisé en interne par `bootstrap` après
  le `poweroff` de la VM, disponible aussi en CLI.
* `beryl wipe <host> --disk=PATH` : wipe destructif (zpool destroy +
  labelclear + sgdisk --zap-all + dd zéros). Demande confirmation
  `OUI`/`YES`. Pour ré-installer sur un disque qui porte déjà un pool
  BSD (garde-fou NOGO de `beryl bootstrap`).
* `Beryl::FreebsdConfig` + `Beryl::UserSpecYaml` : parser du bloc
  `freebsd:` de l'inventaire YAML. Champs : hostname, timezone,
  pool_name, swap_gb, disks, raid, install_type, users, packages,
  sudoers. Valide `raid ∈ {stripe, mirror, raidz, raidz2, raidz3}` et
  `install_type ∈ {distribution_sets, packages}` à la lecture
  (ArgumentError sinon, pas de défaut silencieux).
* `beryl bootstrap <host>` lit `freebsd:` de l'inventaire quand il est
  présent. Priorité : flag CLI > YAML > défaut MVP > erreur explicite.
  Un `--disk` / `freebsd.disks` manquant déclenche une erreur claire
  avant toute action destructive.
* 13 nouveaux specs pour `FreebsdConfig` et son intégration dans
  `Inventory`. Total : 83 specs verts.
* `examples/inventory.yml` : bloc `freebsd:` ajouté à `web01.aloli.fr`
  et exemple complet (`loulou.aloli.net`) avec users, packages, sudoers.

==== Dépendances

* `ovh-api` v0.2.4 — méthode `boot_from_disk(service_name)` (bascule
  OVH inverse de `prepare_rescue`).

=== Sous-commande `rescue` par API hébergeur (avril 2026)

==== Ajouté

* `beryl rescue <host>` : bascule un hôte OVH ou Scaleway en mode
  rescue par l'API de l'hébergeur, puis attend le retour de SSH. Évite
  la séquence clic-dans-le-panel + mail de credentials + `ssh-copy-id`
  qui précédait le `beryl bootstrap`.
  * OVH : enchaîne `boots → find rescue id → set_boot → set_netboot_option
    rescueSshKey → reboot` via `client.dedicated_servers.prepare_rescue`.
  * Scaleway : un appel `servers.reboot(boot_type: Rescue)`. Les clés
    SSH déclarées à la création du serveur sont ré-injectées
    automatiquement par Scaleway.
* Extension de l'inventaire : un hôte peut porter un bloc au nom de son
  provider (`ovh:` ou `scaleway:`) avec les identifiants d'API
  (`service_name`, `ssh_key_name` pour OVH ; `zone`, `server_id` pour
  Scaleway). `Beryl::Host` expose les accesseurs correspondants.
* Helper `Beryl::CLI::Credentials` : lecture des variables d'env
  `OVH_APPLICATION_KEY`, `OVH_APPLICATION_SECRET`, `OVH_CONSUMER_KEY`,
  `OVH_ENDPOINT`, `SCW_SECRET_KEY`, `SCW_DEFAULT_ZONE`, avec messages
  d'erreur francophones.
* Specs : 15 nouveaux specs (inventory provider_config + rescue CLI
  avec transports HTTP stub pour simuler OVH et Scaleway sans réseau).
  Total : 69 specs verts.
* Documentation : nouvelle section 5 dans `TESTING.adoc` et exemples
  `ovh:` / `scaleway:` mis à jour dans `examples/inventory.yml`.

==== Dépendances

* `ovh-api` v0.2.0 (shard `aloli-crystal/crystal-api-ovh`, branche
  `production`) — nouveaux endpoints rescue.
* `scaleway-api` v0.2.0 (shard `aloli-crystal/crystal-api-scaleway`,
  branche `production`) — `servers.reboot` avec `BootType::Rescue`.

=== Pivot bootstrap — ADR-010 / ADR-011 (avril 2026)

Le test grandeur nature du 19 avril 2026 sur un Kimsufi OVH UEFI-only
a révélé que l'ISO mfsBSD `dd`-ée n'est pas amorçable par un firmware
UEFI (ADR-010). Le test manuel du 20 avril a validé end-to-end la
voie de remplacement : FreeBSD est désormais installé *depuis* le
rescue Linux via une VM QEMU en passthrough disque (ADR-011).

==== Ajouté

* `Beryl::Bootstrap::QemuInRescue` : classe unifiée qui remplace le
  couple `MfsBSD` + `Installer`. Orchestre apt → ISO → remasterisation
  xorriso → OVMF → QEMU (-no-reboot, UEFI, KVM, passthrough disque)
  → `reboot -f` → attente SSH.
* Template `templates/installerconfig.sh` : format bsdinstall en deux
  parties (variables ZFSBOOT + distributions pkgbase, puis post-install
  chroot pour `rc.conf` en `ifconfig_DEFAULT`, `fstab` en labels GPT,
  users `admin`/`deploy`, sudoers wheel NOPASSWD, clé SSH, poweroff).
* CLI `beryl bootstrap` : nouveau flag `--iso-url` et
  `--freebsd-version`, et `--installed-user` (défaut `admin`,
  `PermitRootLogin no` respecté).
* Specs : 24 nouveaux specs pour `QemuInRescue` (render, base64, qemu
  command line, argument validation). Total : 54 specs verts.

==== Déprécié

* `Beryl::Bootstrap::MfsBSD` et `Beryl::Bootstrap::Installer` : restent
  compilés pour un éventuel support Legacy BIOS, mais ne sont plus
  appelés par `beryl bootstrap`. En-tête de fichier marqué DEPRECATED
  avec pointeur vers ADR-010 / ADR-011.

==== Documentation

* `ARCHITECTURE.adoc` ADR-003 passé à « Superseded by ADR-011 ».
* `TESTING.adoc` : sections UTM, OVH et Scaleway réécrites pour le
  nouveau flux (plus de `prep-rescue`, plus de `dd` bare metal, simple
  `beryl bootstrap <host>` pendant 25-35 min).

=== Ajouté

==== Fondations du projet

* Squelette initial (shard.yml, LICENSE MIT, README bilingue FR/EN).
* Document d'architecture link:ARCHITECTURE.adoc[ARCHITECTURE.adoc] avec 11 ADR fondateurs :
  FreeBSD 15 pkgbase, agentless SSH, bootstrap mfsBSD + dd (superseded
  par QEMU-in-rescue), recettes YAML composables, conventions Mozilla
  Modern, idempotence.
* Convention Aloli : branche par défaut `production`, `development` après la première version.

==== Phase 1 — Bootstrap Linux → mfsBSD → FreeBSD 15 (MVP)

* `Beryl::SSH` : wrapper agentless autour des binaires `ssh` / `scp`
  systèmes. Connection, Result, CommandFailed, insecure_bootstrap
  (pour la bascule mfsBSD où la clé d'hôte change).
* `Beryl::Inventory` : chargeur YAML d'hôtes avec defaults + overrides,
  `find` / `find?`, construction de `SSH::Connection` depuis un `Host`.
* `Beryl::Bootstrap::MfsBSD` : bascule un rescue Linux vers mfsBSD via
  `dd` + reboot + attente SSH. Image par défaut : depenguin.me FreeBSD 15.
* `Beryl::Bootstrap::Installer` : installe FreeBSD 15 en pkgbase sur
  ZFS (partition EFI + swap + pool zroot). Script shell embarqué à
  la compilation (macro `read_file`), clés SSH en base64.
* `Beryl::CLI` : sous-commandes `list-hosts`, `show`, `bootstrap`, `version`.

==== Tests et documentation

* 28 specs Crystal (SSH, Inventory, Bootstrap) — tous verts.
* link:TESTING.adoc[TESTING.adoc] : guide de test grandeur nature
  (UTM local, OVH dédié, Scaleway Dedibox).
* CI GitHub Actions : format Crystal + specs sur Linux et FreeBSD 15
  via `cross-platform-actions/action`.

=== Prochaines étapes (Phase 2, après retour des tests)

* Ressources idempotentes : `pkg`, `service`, `sysrc`, `user`, `group`,
  `file`, `directory`, `cmd`, `cron`, `sudoers`.
* Ressources DB : `postgresql_role`, `postgresql_db`.
* Durcissement : `sshd_hardening`, `nginx_tls_profile` (profils Mozilla Modern).
* Alerting : `mail_relay` (dma → smarthost), `ntfy_topic` (push HTTP).
* Moteur de recettes YAML (`Beryl::Recipe`) avec variables et inclusions.
* Catalogue de recettes de référence.
